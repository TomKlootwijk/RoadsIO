# Assets

No external art assets are bundled in this prototype.

The current presentation is generated with CSS and Canvas:

- Paint balls are Canvas gradients.
- Splats and particles are Canvas circles.
- The logo is a CSS blob.
- The UI uses CSS glass panels.

This avoids licensing issues and keeps the prototype small. Future art can be added here.
